# M0-T03 — Lints & formatting baseline

- **Milestone:** M0
- **Dependencies:** M0-T02
- **Estimated new code:** S

## Goal
Confirm static analysis and formatting pass cleanly on the scaffold.

## Context — read first
- `analysis_options.yaml` (already in the repo)
- `docs/09-coding-standards.md`

## Steps
1. `dart format lib test` — apply formatting.
2. `flutter analyze --fatal-infos` — fix every issue reported in `lib/` and
   `test/`. The scaffold should already be clean; if a dependency upgrade in
   M0-T02 introduced a lint, resolve it here.

## Acceptance criteria
- [x] `dart format --output=none --set-exit-if-changed lib test` exits 0.
- [x] `flutter analyze --fatal-infos` reports no issues.
- [x] M0-T03 is ticked in `docs/06-task-breakdown.md`.
