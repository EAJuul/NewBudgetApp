# M0-T04 — Folder structure

- **Milestone:** M0
- **Dependencies:** M0-T01
- **Estimated new code:** S

## Goal
Create the layered folder skeleton from `docs/02-architecture.md` so later tasks
have a defined home for every file.

## Context — read first
- `docs/02-architecture.md` — Folder structure

## Files
Create these directories, each containing a `.gitkeep` file (Git does not track
empty directories):
```
lib/core/money/        lib/core/time/        lib/core/error/
lib/core/extensions/
lib/data/database/     lib/data/database/tables/   lib/data/daos/
lib/domain/budgeting/
lib/features/<feature>/domain/
lib/features/<feature>/data/
lib/features/<feature>/presentation/
  for <feature> in: accounts, transactions, budget, categories, payees,
  targets, reports, import_csv, reconciliation, settings, onboarding
integration_test/
```

## Acceptance criteria
- [ ] The directory tree matches `docs/02-architecture.md`.
- [ ] `flutter analyze` still passes.
- [ ] M0-T04 is ticked in `docs/06-task-breakdown.md`.
